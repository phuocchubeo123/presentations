\begin{frame}
    \frametitle{Results Analysis}
    Matching Phase:
    \begin{itemize}
        \item All related works (including ours) use tree-searching technique to find matching elements.
        \item Total sender communication - receiver computation cost is $O(u^d)$. \tikz[remember picture] \coordinate (complexity);
    \end{itemize}
    \visible<2>{
    \begin{tikzpicture}[remember picture,overlay]
        \node[align=center] (comment) at ([xshift=-0.3cm, yshift=-1cm]complexity.south) {\scriptsize all possible prefix combinations!};
        \draw[->,thick] (comment.north) -- ([xshift=-0.5cm, yshift=-2pt]complexity.south);
    \end{tikzpicture}
    }
    \only<1, 2>{
        \begin{tikzpicture}[
            level distance=1.4cm,
            every node/.style={circle,draw,minimum size=6mm},
            level 1/.style={sibling distance=5.6cm},
            level 2/.style={sibling distance=2.8cm},
            level 3/.style={sibling distance=1.4cm,minimum size=5mm}
        ]
            \node {}
                child { node {}
                    child { node {}
                        child { node {} }
                        child { node {} }
                    }
                    child { node {}
                        child { node {} }
                        child { node {} }
                    }
                }
                child { node {}
                    child { node {}
                        child { node {} }
                        child { node {} }
                    }
                    child { node {}
                        child { node {} }
                        child { node {} }
                    }
                };
        \end{tikzpicture}
    }
    \only<3>{
    Fuzzy Map:
    \begin{itemize}
        \item The fuzzy map step in \cite{van2025fuzzy,zhang2025fast,piske2025distance} has communication complexity depends \textit{linearly} on $u \approx \delta$ (the distance).
        \item Function Secret Sharing in \cite{garimella2024computation} has comm. $O({\color{red} \kappa^2} \cdot N_A \cdot \log \delta \cdot d)$.
        \item {\color{brown} This work:} Communication $O({\color{red} \kappa} \cdot N_A \cdot \log \delta \cdot d)$.
    \end{itemize}
    }
\end{frame}
